customCategory: leashed_change
name: leashed_change_take_target
description: "{{render_template(\"leash_actions/leash_action_take_target\")}}"
questEditorId: SkyrimNet_Leashed
scriptName: SkyrimNet_Leashed_Actions
executionFunctionName: TakeLeash
parameterMapping:
  - type: speaker
    name: subject
    description: Speaker taking hold of an existing leash
  - type: dynamic
    name: leashed
    description: Display name of the collared actor whose existing leash is taken, exactly as listed under available targets.
    useNearbyActors: false
enabled: true
eligibilityRules:
  - conditions:
      - decoratorName: collared_nearby
        arguments:
          - currentActor
        comparisonOperator: ==
        expectedValue: "available"
